# Done to increase the memory available to gradle.
    org.gradle.jvmargs  = -Xmx6G
    org.gradle.parallel = true

# Fabric Properties
    # check these on https://fabricmc.net/develop
    minecraft_version   = 1.19.3
    yarn_mappings       = 1.19.3+build.5
    loader_version      = 0.14.14

# Dependencies
    # Check on https://fabricmc.net/develop
    # fabricapi_version   = insert version here

    # Check on https://github.com/LlamaLad7/MixinExtras/releases
    mixinextras_version = 0.1.1

# Mod Properties
    mod_id              = template-mod
    mod_version         = 1.0.0
    mod_name            = Template Mod
    mod_description     = A template for a mod!
    mod_license         = MIT
    mod_main_class      = TemplateMod
    supported_minecraft = ~1.19
    group               = top.offsetmonkey538.templatemod
